const model = {
    app: {
        currentPage: null,
        turn: "circle",
        winner: null,
    },
    fields: {
        // 0 = blank, 1 = circle, 2 = cross
        grid: [0,0,0,0,0,0,0,0,0],
    }
}